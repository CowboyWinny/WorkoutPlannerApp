BEGIN TRANSACTION;
CREATE TABLE IF NOT EXISTS "DayPlan" (
	"ID"	INTEGER NOT NULL UNIQUE,
	"DayOfWeek"	TEXT,
	"Date"	TEXT,
	"ExcerciseID"	INTEGER NOT NULL,
	"Rounds"	INTEGER NOT NULL DEFAULT 1,
	"Repeats"	INTEGER NOT NULL DEFAULT 1,
	PRIMARY KEY("ID" AUTOINCREMENT),
	FOREIGN KEY("ExcerciseID") REFERENCES "Excercises"("ID")
);
CREATE TABLE IF NOT EXISTS "Excercises" (
	"ID"	INTEGER NOT NULL UNIQUE,
	"Name"	TEXT NOT NULL,
	"CaloriesCost"	INTEGER NOT NULL DEFAULT 0,
	"Description"	TEXT,
	PRIMARY KEY("ID" AUTOINCREMENT)
);
CREATE TABLE IF NOT EXISTS "TagsForExcercises" (
	"ID"	INTEGER NOT NULL UNIQUE,
	"ExcerciseID"	INTEGER NOT NULL,
	"Arms"	INTEGER NOT NULL DEFAULT 0,
	"Legs"	INTEGER NOT NULL DEFAULT 0,
	"Back"	INTEGER NOT NULL DEFAULT 0,
	"Core"	INTEGER NOT NULL DEFAULT 0,
	"IsIsolated"	INTEGER NOT NULL DEFAULT 0,
	"IsBallistic"	INTEGER NOT NULL DEFAULT 0,
	PRIMARY KEY("ID" AUTOINCREMENT),
	FOREIGN KEY("ExcerciseID") REFERENCES "Excercises"("ID")
);
COMMIT;
